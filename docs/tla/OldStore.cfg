\* Isolates counterexample (b): only rule 2 is broken, evict is off.
SPECIFICATION Spec
CONSTANTS
    HandlerSteps = 2
    NewCloseRules = TRUE
    NewStoreRules = FALSE
    EvictOn = FALSE
    WedgeK1 = FALSE
INVARIANT TypeOK
PROPERTIES AllDone AllCullsDone
